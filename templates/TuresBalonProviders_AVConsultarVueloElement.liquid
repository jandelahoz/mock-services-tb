<soapenv:Envelope xmlns:soapenv="http://schemas.xmlsoap.org/soap/envelope/" xmlns:typ="http://servicios.avianca.com/types/">
   <soapenv:Header/>
   <soapenv:Body>
      <typ:consultarVueloResponseElement>
         <!--Zero or more repetitions:-->
         <typ:result>
            <typ:fechaSalida>{{ data.fechaSalida }}</typ:fechaSalida>
            <typ:fechaLlegada>{{ data.fechaLlegada1 }}</typ:fechaLlegada>
            <typ:ciudadOrigen>{{ data.ciudadOrigen }}</typ:ciudadOrigen>
            <typ:vuelo>{{ data.number1 }}</typ:vuelo>
            <typ:clase>{{ data.clase }}</typ:clase>
            <typ:ciudadDestino>{{ data.ciudadDestino }}</typ:ciudadDestino>
            <typ:precio>{{ data.precio1 }}</typ:precio>
         </typ:result>
      </typ:consultarVueloResponseElement>
   </soapenv:Body>
</soapenv:Envelope>